query getMe {
  me {
    id
    firstName
    lastName
    email
    role
  }
}

mutation Login($input: LoginUserInput!) {
  login(input: $input) {
    success
    user {
      id
      firstName
      lastName
      email
      role
    }
  }
}

mutation Register($input: CreateUserInput!) {
  register(input: $input) {
    success
    user {
      id
      firstName
      lastName
      email
      role
    }
  }
}

mutation Logout {
  logout
}
